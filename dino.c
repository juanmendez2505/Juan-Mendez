#include <stdio.h>
#include <stdbool.h> // Incluye la biblioteca stdbool.h

int main() {
    bool esMayorDeEdad = false; // Declaración y asignación de una variable bool

    if (esMayorDeEdad) 
    {
        printf("La persona es mayor de edad.\n");
    } else {
        printf("La persona es menor de edad.\n");
    }

    return 0;
}
